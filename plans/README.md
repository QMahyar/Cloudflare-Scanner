# Implementation Plans

Generated by the `improve` skill on 2026-07-14 and implemented directly (all 16
plans committed to master through commit `dd64010`).

**Baseline at authoring time**: `go vet ./...` clean, `go test ./...` passes. The
race detector could not run locally (no cgo/gcc) and CI does not run `-race` either —
noted for the maintainer, not a plan.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Fix ui/dist build contract + correct docs | P1 | S | LOW | — | DONE |
| 002 | Harden shutdown path (cancel jobs, tear down SSE, clean log) | P1 | S | LOW | — | DONE |
| 003 | Cap & dedupe clean-scan port list | P1 | S | LOW | — | DONE |
| 004 | Test replacer dedup / ConfigFingerprint | P1 | S | LOW | — | DONE |
| 005 | Extract shared xray batch-config builder | P2 | M | MED | — | DONE |
| 006 | Extract shared batch-orchestration runner | P2 | M | MED | 005 (coordinate) | DONE |
| 007 | Thread ctx into native WARP probe (responsive Stop) | P2 | M | LOW-MED | — | DONE |
| 008 | Round-trip tests for REALITY/xTLS/gRPC share URLs | P2 | M | LOW | — | DONE |
| 009 | Reject control chars in apply-endpoint value | P2 | S | LOW | — | DONE |
| 010 | Split server.go into cohesive files | P3 | M | LOW | 002,003,005,006 | DONE |
| 011 | Split cleanip.go into gen/measure/validate/run | P3 | M | LOW | 003,006 | DONE |
| 012 | Hoist shared failEntry DTO + align FE throttle | P3 | S | LOW | 010 (prefer) | DONE |
| 013 | gofmt/lint CI gate + normalize .go EOL to LF | P3 | M | MED | 010,011 (prefer) | DONE |
| 014 | Refresh IMPROVEMENTS.md to shipped reality | P3 | S | LOW | 001 (consistency) | DONE |
| 015 | Replace real-looking key in sample.conf | P3 | S | LOW | — | DONE |
| 016 | Protocol-neutral clean-IP export (design/spike) | P3 | M | LOW-MED | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Implementation notes (post-hoc)

- All 16 plans landed as one commit series on `master`, roughly in dependency order
  (001→009, then 015/014 housekeeping, then 016, then the file splits 010/011,
  then 012/013). `git log --oneline` on `master` has the full trail.
- Plan 013's CI gofmt step was initially missed during the split-heavy stretch (010/011
  moved code through several files after the EOL-normalization commit, reintroducing a
  few small formatting nits); caught and fixed in a follow-up commit alongside the
  actual CI gate addition. `gofmt -l .` is clean at HEAD.
- Plan 015: the compromised key was also scrubbed from the two test files that had
  reused it as throwaway key material, and replaced with a freshly generated
  keypair. The original key remains in git history — see the plan for the rotation
  recommendation.


## Recommended sequencing

- **Do first (P1, all Small, independent):** 001, 002, 003, 004. High leverage, low
  risk, clean verification. 002 fixes recently-added uncommitted shutdown code — verify
  its drift check carefully since that code is in flux.
- **Then P2:** 007 and 009 (bug/security, independent) anytime. 005 → 006 are the
  refactors; do **005 before 006** (both touch the xray batch area; 006 assumes the
  builder seam is settled). 008 (tests) anytime.
- **Then P3 refactors:** run **005/006 before 010/011** so the file-splits relocate a
  single builder/runner, not two. Run **010/011 before 012 and 013** so DTO-hoisting
  and EOL-normalization land on the final file layout (avoids a painful renormalize
  collision). 014 and 015 are standalone housekeeping. 016 is the forward-looking item.

## Dependency notes

- **006 after 005**: both edit the xray batch machinery (`xray.go`/`proxy.go`/`cleanip.go`);
  sequencing avoids overlapping edits and lets 006 build on the unified config builder.
- **010/011 after 002,003,005,006**: those plans edit `server.go`/`cleanip.go` bodies;
  moving the code first would force the executors of 002/003/005/006 to rebase onto a new
  layout. Split last.
- **012 after 010**: the shared `failEntry` type slots naturally into the post-split layout.
- **013 after 010,011**: the one-time `.go` EOL renormalization rewrites every Go file's
  bytes; do it once the files have stopped moving, or it collides with the splits.
- **014 references 001**: keep IMPROVEMENTS.md consistent with the corrected build docs.

## Findings considered and rejected

Recorded so they aren't re-audited next run:

- **Results-response re-serialization / host re-parsing** (`server.go:handleScanResults`
  rebuilds entries+raw+failures each poll; `ipOnly` re-parses per row): real redundant
  work, but this is a localhost single-user tool polling a few times/second — the cost is
  negligible. **Not worth doing.**
- **Dependency upgrades**: `quic-go v0.60.0`, `golang.org/x/crypto v0.53.0`, and Go 1.26
  are all current; no EOL/security lag. Nothing to do.
- **`gofmt -l` flags all 14 Go files**: this is purely a CRLF line-ending artifact
  (Windows checkout), not real formatting drift — folded into plan 013 (EOL normalization),
  not a standalone finding.
- **SSRF / DNS-rebinding, path traversal, CSRF, CSP, xray process leaks, worker-pool
  races**: audited and found **already well-hardened** (the `subscriptionHTTPClient`
  `Control` hook defeats rebinding TOCTOU; `handleApplyEndpoint` has correct
  `filepath.Rel`+`Base` guards; CSRF token + loopback-Host check present; `BatchProbe`
  correctly Kill/Wait's children on every path). No findings. (Plan 009 tightens one
  *residual* input-validation gap — control chars in the endpoint value — not a hardening regression.)

## What was NOT audited

- The **live network / xray runtime paths** (actual handshakes, real 204 checks, real
  QUIC egress) — not unit-testable without network + the xray sidecar; verified by reading only.
- **Race detector**: could not run locally (no cgo toolchain). Recommend the maintainer
  run `CGO_ENABLED=1 go test -race ./...` on a machine with a C compiler as a one-time check.
- **Frontend runtime behavior / visual UI** — audited `stores.js`/`app.css`/component
  source statically (app.css is 2,387 lines but not obviously bloated; no finding raised);
  no browser/E2E pass.
- **Deep Svelte 5 rune correctness** beyond the documented VirtualTable workaround.
