# شروع کار

> **قدم ۰ — نصب برنامه:** برای راهنمای دانلود، extract و اجرای مخصوص هر سیستم‌عامل به [README.fa.md](../../README.fa.md#راهنمای-سیستم‌عامل‌ها) مراجعه کنید.

---

## گردش کار اولیه

این مراحل فرض می‌کنند برنامه در حال اجراست و رابط وب را در `http://127.0.0.1:XXXXX` می‌بینید.

### قدم ۱ — باز کردن رابط وب

یک برگه مرورگر باید auto در `http://127.0.0.1:XXXXX` باز شود (پورت هر بار تغییر می‌کند).

اگر باز نشد، خروجی ترمینال را نگاه کنید. خطی شبیه این می‌بینید:

```
Web UI: http://127.0.0.1:53671
```

آن آدرس را کپی کنید و در مرورگر بچسبانید.

### قدم ۲ — تغییر زبان

روی دکمه **English** در گوشه بالا-راست کلیک کنید تا بین انگلیسی و فارسی جابجا شوید.

### قدم ۳ — دریافت کانفیگ واپ

در برگه **Endpoint Scanner** به پایین اسکرول کنید. برنامه شامل لینک‌های ژنراتورهای آنلاین، بات‌های تلگرام و ابزارهای خط فرمان است.

### قدم ۴ — اسکن اندپوینت‌های واپ

۱. فایل `.conf` خود را آپلود کنید
۲. تعداد اسکن را انتخاب کنید (Quick = ۱۰۰ برای بار اول کافی است)
۳. **Start Scan** را بزنید
۴. نتایج زنده نمایش داده می‌شوند — سریع‌ترین را انتخاب کنید

### قدم ۵ — اعمال اندپوینت

۱. روی اندپوینت در جدول نتایج کلیک کنید
۲. فایل(های) `.conf` خود را انتخاب کنید
۳. **Generate Configs** را بزنید
۴. از کانفیگ تغییریافته در کلاینت Warp خود استفاده کنید

---

## محتویات فایل فشرده

| فایل | توضیح |
|---|---|
| `Cloudflare-Scanner` (یا `.exe`) | برنامه اصلی — سرور وب + موتور اسکن |
| `xray` (یا `xray.exe`) | xray-core نسخه v1.8.24 — اعتبارسنجی واپ + اتصالات پروکسی |

هر دو فایل لازم هستند. آنها را در یک پوشه نگه دارید.

---

## بستن برنامه

پنجره ترمینال را ببندید، یا **Ctrl+C** بزنید.

---

## مراحل بعدی

- [اسکنر اندپوینت](endpoint-scanner.md) — راهنمای کامل با تمام گزینه‌ها
- [اسکنر آی‌پی](ip-scanner.md) — پیدا کردن آی‌پی‌های پروکسی تمیز کلاودفلر
- [جایگزین آی‌پی](ip-replacer.md) — جایگزینی دسته‌جمعی آی‌پی در کانفیگ‌ها
- [سوالات متداول](faq.md) — عیب‌یابی و سوالات رایج
