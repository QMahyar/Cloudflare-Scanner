# Build artifacts
dist/
# Local dev build + run loop output (scripts/dev.ps1): per-platform binaries,
# the cached xray sidecar, everything throwaway.
builds/
# Frontend bundle: built from frontend/ via `npm run build`, CI builds it before go build
ui/dist/
Cloudflare-Scanner.exe
WarpEndpointScanner.exe
*.exe
!*.go

# Frontend toolchain
frontend/node_modules/

# Local xray-core runtime artifacts
_xray_work/

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
.DS_Store

# User configs
WARPGermany.conf

# Go workspace
go.work
go.work.sum

dedup_test_data.txt
xray.exe
xray
verify_*
cmd/verify/

# Claude Code local config / session artifacts (settings.local.json holds
# machine-specific paths + permissions; workflows are personal tooling)
.claude/

# Playwright MCP page snapshots (transient debugging artifacts)
.playwright-mcp/

# Serena MCP local project config / cache
.serena/

# Local UI debugging scratch (screenshots, RTL probes)
.shots/
frontend/debug/
frontend/debug-rtl.html
