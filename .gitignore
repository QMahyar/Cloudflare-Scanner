# Build artifacts
dist/
# ...except the embedded frontend bundle, which is committed so `go build`
# works with no Node toolchain (built from frontend/ via `npm run build`).
!ui/dist/
!ui/dist/**
Cloudflare-Scanner.exe
WarpEndpointScanner.exe
*.exe
!*.go

# Frontend toolchain
frontend/node_modules/

# Local xray-core runtime artifacts
_xray_work/

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
.DS_Store

# User configs
WARPGermany.conf

# Go workspace
go.work
go.work.sum

dedup_test_data.txt
xray.exe
xray
verify_*
cmd/verify/
